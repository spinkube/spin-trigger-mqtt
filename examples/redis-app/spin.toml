spin_manifest_version = "1"
authors = ["Suneet Nangia <suneetnangia@gmail.com>"]
description = ""
name = "redis-app"
trigger = { type = "redis", address = "redis://localhost:6379" }
version = "0.1.0"

[[component]]
id = "redis-app"
source = "target/wasm32-wasi/release/redis_app.wasm"
allowed_http_hosts = []
[component.trigger]
channel = "messages-in"
[component.build]
command = "cargo build --target wasm32-wasi --release"
